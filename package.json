{
  "name": "local-crawler",
  "version": "0.1.0",
  "description": "Core local website content extraction engine (developer-owned, local dev servers only)",
  "main": "index.js",
  
  
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:watch": "vitest",
    "test-app": "tsx test-app/server.ts",
    "test-crawler": "tsx test-app/crawler-test.ts"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "playwright": "^1.47.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.23.12",
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  }
}
